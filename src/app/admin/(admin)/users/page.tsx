import { UserManagementPage } from '@/components/features'

export const metadata = {
  title: 'Vigroup | Quản lý người dùng',
}

const page = () => {
  return <UserManagementPage />
}

export default page
